node('jenkins-slave') {
    
     stage('test pipeline') {
        container("aws"){
            sh(script: """
                echo "hello"
                terraform --version
                aws --version

            """)
        }
    }
}
