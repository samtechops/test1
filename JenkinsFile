node('jenkins-slave') {
    
     stage('test pipeline') {
        sh(script: """
            echo "hello"
            tfenv install 1.1.7
            tfenv use 1.1.7
            aws --version
           
        """)
    }
}
